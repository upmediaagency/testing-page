<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>🔥 Get Your Free Ultimate Weight Loss eBook! 🔥</title>
  <!-- FontAwesome for icons -->
  <script src="https://kit.fontawesome.com/a076d05399.js" crossorigin="anonymous"></script>
  <style>
    /* Import Google Fonts */
    @import url('https://fonts.googleapis.com/css2?family=Playfair+Display:wght@400;600;700&family=Poppins:wght@300;400;600&family=Orbitron:wght@500&display=swap');

    body {
      font-family: 'Poppins', sans-serif;
      background: linear-gradient(135deg, #fdfbfb, #ebedee);
      margin: 0;
      padding: 20px;
      display: flex;
      flex-direction: column;
      justify-content: center;
      align-items: center;
      min-height: 100vh;
      position: relative;
      overflow-y: auto;
    }

    /* Creative background shapes */
    .background-deco, .background-deco-2 {
      position: absolute;
      z-index: -1;
      filter: blur(50px);
    }
    .background-deco {
      width: 400px;
      height: 400px;
      background: radial-gradient(circle at center, #ff9a9e, transparent);
      top: -150px;
      left: -150px;
    }
    .background-deco-2 {
      width: 300px;
      height: 300px;
      background: radial-gradient(circle at center, #fad0c4, transparent);
      bottom: -100px;
      right: -100px;
    }

    .container {
      background: #fff;
      border-radius: 20px;
      padding: 40px;
      max-width: 500px;
      width: 100%;
      text-align: center;
      box-shadow: 0 15px 35px rgba(0, 0, 0, 0.1);
      border: 1px solid #ececec;
      position: relative;
      margin: 20px auto;
      overflow: hidden;
    }
    .container::before {
      content: "";
      position: absolute;
      top: 0;
      left: 0;
      width: 100%;
      height: 100%;
      background: linear-gradient(45deg, rgba(255,255,255,0.2), rgba(0,0,0,0));
      pointer-events: none;
    }

    h1 {
      font-family: 'Playfair Display', serif;
      font-size: 36px;
      margin-bottom: 20px;
      color: #333;
      background: linear-gradient(45deg, #ff6a00, #ee0979);
      -webkit-background-clip: text;
      -webkit-text-fill-color: transparent;
    }

    p {
      font-size: 18px;
      color: #555;
      margin-bottom: 30px;
    }

    .ebook-image {
      max-width: 60%;
      border-radius: 15px;
      box-shadow: 0 10px 25px rgba(0, 0, 0, 0.15);
      margin-bottom: 30px;
    }

    /* Premium Countdown Timer */
    .premium-timer {
      background: #1a1a1a;
      border-radius: 12px;
      padding: 20px;
      margin: 20px auto 0;
      max-width: 300px;
      position: relative;
      box-shadow: 0 0 20px rgba(0, 255, 255, 0.3);
    }
    .premium-timer::before {
      content: "";
      position: absolute;
      top: -5px;
      left: -5px;
      right: -5px;
      bottom: -5px;
      border-radius: 12px;
      background: linear-gradient(45deg, #00f5ff, #00d4ff);
      z-index: -1;
      filter: blur(8px);
    }
    .premium-timer-label {
      font-size: 16px;
      color: #00d4ff;
      margin-bottom: 10px;
      text-transform: uppercase;
      letter-spacing: 2px;
    }
    .premium-timer-display {
      font-family: 'Orbitron', sans-serif;
      font-size: 48px;
      font-weight: 500;
      color: #00f5ff;
      text-shadow: 0 0 10px rgba(0, 213, 255, 0.8);
    }

    @media (max-width: 600px) {
      h1 { font-size: 28px; }
      p { font-size: 16px; }
      .ebook-image { max-width: 80%; }
      .premium-timer-display { font-size: 36px; }
    }
  </style>
</head>
<body>
  <div class="background-deco"></div>
  <div class="background-deco-2"></div>
  <div class="container">
    <h1>🎁 Get Your Free Ultimate Weight Loss eBook! 🎁</h1>
    <img src="free ebook.png" alt="Free eBook" class="ebook-image">
    <p>Unlock the secrets to burning fat and achieving your dream body!</p>

    <!-- Brevo Form Embedded via iFrame -->
    <iframe width="540" height="305" 
            src="https://49df9fbb.sibforms.com/serve/MUIFABxNp6vGBEPtY-8TVWnzwzo9Zg2GPfoT2SS0EFthz6FqElv9ZhfcY_hU6I03jerOJptdz3QPvzhdT3nN1wRZptDEphslKS2JWx4sJ_EbBcbbV2Fm4bxJ5evqHff0zYEXCLYGVq3aleWp1JKaQL_j37Vzuclc4AQR5ZnnV9h63OxtStGclvt7NzdRGUvS6zLZ8jsAbBECO_21" 
            frameborder="0" scrolling="auto" allowfullscreen 
            style="display: block; margin-left: auto; margin-right: auto; max-width: 100%;">
    </iframe>

    <!-- Premium Countdown Timer -->
    <div class="premium-timer">
      <div class="premium-timer-label">Offer Expires In</div>
      <div class="premium-timer-display">
        <span id="premium-countdown">15:00</span>
      </div>
    </div>
  </div>

  <script>
    // Real 15-minute countdown timer (900 seconds)
    let premiumTimeLeft = 15 * 60;
    const premiumCountdownElement = document.getElementById('premium-countdown');

    function updatePremiumCountdown() {
      if (premiumTimeLeft >= 0) {
        const minutes = Math.floor(premiumTimeLeft / 60);
        const seconds = premiumTimeLeft % 60;
        premiumCountdownElement.textContent = `${minutes < 10 ? '0' + minutes : minutes}:${seconds < 10 ? '0' + seconds : seconds}`;
        premiumTimeLeft--;
      } else {
        premiumCountdownElement.textContent = "00:00";
        clearInterval(premiumInterval);
      }
    }
    const premiumInterval = setInterval(updatePremiumCountdown, 1000);
  </script>
</body>
</html>
